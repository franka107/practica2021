import MUIDataTable, { MUIDAtaTableProps } from "mui-datatables";

/**
 * @param {MaterialTableProps} props
 */
function CustomMuiTable(props) {
  return <MUIDataTable {...props} />;
}

export default CustomMuiTable;
